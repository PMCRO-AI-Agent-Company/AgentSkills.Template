# Workflows

Place project-scoped dynamic workflow scripts here.

Each script file becomes a `/<name>` command at session start.

Workflows are usually **saved from a live run** rather than authored from scratch. A project workflow takes precedence over a personal one with the same name.

See: [PMCR-O Workflows documentation](/en/workflows)
