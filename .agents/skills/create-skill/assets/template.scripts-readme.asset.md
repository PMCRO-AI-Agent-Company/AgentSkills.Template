# Template: scripts/README.md

Copy into a new skill's `scripts/README.md` as a stub when no
executable scripts exist yet.

```markdown
# Scripts — <skill-name>

No scripts yet. When this skill needs executable code the agent can
run (not just documentation), add it here and list it below:

| Script | Purpose | How to invoke |
|--------|---------|----------------|
| `<script-name>` | <what it does> | `<command>` |
```
