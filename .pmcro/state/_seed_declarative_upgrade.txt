Complete the declarative-generative-agent-template upgrade that Shawn approved in a separate (ChatGPT) session and that session started but could not finish ("repository edit session is currently not executing the generator upgrade cleanly, so I'm stopping before claiming the change is complete"). Found uncommitted on disk: .pmcro/design/DECLARATIVE-GENERATIVE-AGENT-TEMPLATE.md (design doc), .pmcro/design/templates/base-agent-scaffold.yaml, examples/generative-domain-agent.spec.yaml, a modified plugins/pmcro-marketplace-directory/skills/scaffold-skill/assets/schemas/scaffold-spec.schema.json (adds laws/permissions/output/reasoning/evaluation fields), and a prepared-but-never-run one-time patch script (scripts/_upgrade_generator.py + run_upgrade.ps1) that rewrites scaffold.py's render_agentskills() to actually project those new fields into generated SKILL.md content instead of only accepting them in the schema. Shawn confirmed live, in-session, that he approved this direction and wants it finished ("fully setup autonomously as long as you can build project").
