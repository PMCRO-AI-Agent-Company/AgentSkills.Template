apiVersion: pmcro.ai/v1
kind: AgentScaffoldSpec
metadata:
  id: eval-maf-inline-ok
  kind: domain
  display_name: Eval Maf Inline Ok
spec:
  description: Valid maf-inline C# fixture used by the scaffold-skill evaluation suite.
  packaging:
    - target: maf-inline
      language: csharp
      path: src/Agents/eval-maf-inline-ok
  skills:
    - name: execute
      description: Primary MAF inline entry point for evaluation.
